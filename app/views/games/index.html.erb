<h1>Available Games</h1>
<ul>
  <% if @games.present? %>
    <% @games.each do |game| %>
      <li><%= link_to game.title, game_path(game) %></li>
    <% end %>
  <% else %>
    <li>No games available.</li>
  <% end %>
</ul>